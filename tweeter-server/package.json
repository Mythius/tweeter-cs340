{
  "name": "tweeter-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && tsc",
    "clean": "rimraf dist",
    "update-layer": "mkdir -p layer/nodejs/node_modules && cp -r ../tweeter-shared/dist layer/nodejs/node_modules/tweeter-shared",
    "deploy": "npm run build && npm run update-layer && sam build && echo 'y' | sam deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "tweeter-shared": "file:../tweeter-shared",
    "date-fns": "^3.3.1",
    "uuid": "^9.0.1"
  }
}
